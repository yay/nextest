export default function EconomyNotFound() {
  return (
    <div>
      {
        "'Economy' not found element. This doesn't seem to work. Only root level 'not-found.tsx' works."
      }
    </div>
  );
}
